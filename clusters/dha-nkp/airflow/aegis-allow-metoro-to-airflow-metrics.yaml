apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: aegis-allow-metoro-to-airflow-metrics
  namespace: aegis-airflow
  labels:
    app.kubernetes.io/component: monitoring
    app.kubernetes.io/part-of: aegis
    aegis.io/phase: "8"
spec:
  podSelector:
    matchLabels:
      component: statsd
      release: aegis-airflow-airflow
      tier: airflow
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: metoro
          podSelector:
            matchLabels:
              app.kubernetes.io/component: service-monitor-scraper
      ports:
        - protocol: TCP
          port: 9102
